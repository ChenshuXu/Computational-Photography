install cvxpy from conda with command "conda install -c conda-forge cvxpy"
update numpy to a higher version, command: "conda install -c anaconda numpy=1.19.1"

At the same level of main.py and final_project.py, create a folder named "videos".
Inside this "videos" folder create a folder named "source",
Then put all input videos into this "source" folder and run main.py